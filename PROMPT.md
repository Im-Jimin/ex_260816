# AI 분리배출 가이드 — 웹 서비스 구현 지시서

> 이 파일을 프로젝트 루트에 `PROMPT.md`로 저장한 뒤, Claude Code에서
> `PROMPT.md 읽고 Phase 1부터 순서대로 구현해줘` 라고 시작하면 됩니다.

---

## 0. 프로젝트 한 줄 정의

**쓰레기의 "이름과 종류"가 아니라, 사용자가 "지금 바로 해야 할 행동"을 알려주는 분리배출 가이드 웹 서비스.**

기존 서비스가 "이건 플라스틱입니다"에서 끝난다면, 이 서비스는
"① 내용물 비우기 → ② 물로 헹구기 → ③ 라벨 떼기 → ④ 압착 후 무색페트 전용함"
처럼 **실행 가능한 체크리스트**로 끝난다. 이 차이가 프로젝트의 전부다.

---

## 1. 해결하려는 문제 (기획 배경)

| 문제 | 구체적 상황 |
|---|---|
| 복합 재질의 난해함 | 치약 튜브, 기름진 배달 용기, 테이프 붙은 스티로폼 — 재질과 오염도 판단이 어려움 |
| 지자체별 상이한 기준 | 전국 공통 기준과 우리 동네 수거 방식이 달라 검색 피로도·신뢰도 하락 |
| 실시간 대응의 한계 | 분리수거함 앞에서 긴 글을 읽거나 여러 사이트를 대조하는 비효율 |
| 행동 단위 안내의 결핍 | 재질 분석은 있어도 "비우고, 헹구고, 분리하고" 같은 시퀀스가 없음 |
| 주관적 판단에 의한 오배출 | 명확한 가이드가 없어 재활용 자원을 그냥 일반쓰레기로 투기 |

**주요 타깃 사용자**: 1인 가구, 사회초년생, 배달음식 고빈도 이용층, 자녀 ESG 교육 목적 가정

**서비스가 지켜야 할 3가지 원칙 (UI/문구 전반에 반영할 것)**
1. **행동 언어** — 규정 나열("합성수지 용기류에 해당합니다") 금지. 동사 명령형("헹구세요")으로 쓴다.
2. **정직한 AI** — 확실하지 않으면 단정하지 않고 **되묻는다**. ("겉면에 금속 코팅이 있나요?")
3. **순환 경제 연결** — 완료를 포인트·탄소 절감량으로 환산해 성취감을 준다.

---

## 2. 구현 범위 (중요 — 이 선을 넘지 말 것)

### ✅ 구현한다
- 텍스트 검색 → 품목별 배출 가이드 (핵심 기능)
- 17개 카테고리 아이콘 그리드 → 카테고리별 품목 목록
- 단계별 행동 체크리스트 (체크 시 진행률 표시)
- 모호한 경우 되묻는 질문 → 답변에 따라 가이드 분기
- 완료 시 포인트 적립 + 탄소 절감량 누적 (localStorage)
- 지역 선택 드롭다운(시/도 단위) → 지역별 예외 안내 문구 표시

### ❌ 구현하지 않는다 (과제 범위 밖 / 과도한 복잡도)
- **사진 촬영 AI 비전 분석** — 제안서에는 있지만 MVP에서 제외. 대신 검색창에 카메라 아이콘 버튼만 두고 클릭 시 "준비 중" 툴팁 표시.
- **GPS 위치 기반 자동 감지** — 드롭다운 수동 선택으로 대체
- **회원가입 / 로그인 / 서버 DB** — 전부 localStorage
- **지자체 수거일정 API 연동, 지도, 포인트 현금화, 랭킹**

> 제안서에 나오지만 위 목록에 있는 기능은 **절대 임의로 추가하지 마라.** 필요하다고 판단되면 구현 대신 나에게 먼저 물어봐라.

---

## 3. 기술 스택

- **Next.js 15 (App Router) + TypeScript**
- **Tailwind CSS v4**
- 아이콘: **lucide-react** (레퍼런스 사이트의 아이콘 에셋을 그대로 쓰지 말고 lucide로 대체)
- 상태: React state + localStorage. 상태관리 라이브러리 도입 금지.
- 데이터: `data/items.json` 로컬 JSON. DB 없음.
- AI: Phase 3에서 `/api/ask` route handler + `@anthropic-ai/sdk`, 키는 `.env.local`의 `ANTHROPIC_API_KEY`. **API 키를 클라이언트 컴포넌트에서 절대 참조하지 마라.**

---

## 4. UI 사양

전체 톤: **여백이 넉넉하고 요소가 적은 화면.** 배너, 공지, 뉴스, 통계 위젯 같은 부가 요소를 만들지 마라. 화면에는 검색바와 카테고리 그리드만 있다.

### 4-1. 디자인 토큰 (`globals.css`에 CSS 변수로 정의)

```css
--bg:            #F6F9FE;   /* 페이지 배경 (아주 옅은 블루그레이) */
--surface:       #FFFFFF;   /* 카드 배경 */
--text:          #1F2A44;   /* 기본 텍스트 (짙은 네이비) */
--text-muted:    #8A93A6;   /* placeholder, 보조 텍스트 */
--grad-start:    #3773D9;   /* 그라데이션 시작 (블루) */
--grad-mid:      #239FA0;   /* 중간 (틸) */
--grad-end:      #1DAD88;   /* 끝 (그린) */
--radius-card:   20px;
--shadow-card:   0 4px 16px rgba(31, 42, 68, 0.06);
```

그라데이션은 항상 `linear-gradient(90deg, var(--grad-start), var(--grad-mid), var(--grad-end))` 로 통일.

### 4-2. 검색바 (히어로 영역)

- 화면 상단 중앙, 최대 폭 `720px`, 완전한 **pill 형태** (`border-radius: 9999px`)
- **테두리 자체가 그라데이션**: 내부는 흰 배경, 테두리 2px만 좌→우 블루→그린 그라데이션.
  구현법: 바깥 div에 그라데이션 배경 + `padding: 2px` + pill radius, 안쪽 div에 흰 배경 + pill radius. (`border-image`는 radius와 함께 깨지므로 쓰지 마라.)
- 오른쪽 끝에 **원형 그라데이션 버튼**. 지름은 검색바 높이보다 약간 커서 바깥으로 살짝 튀어나오도록 배치하고, 안에 흰색 돋보기 아이콘.
- placeholder: `내가 가진 쓰레기 어떻게 버려야 할까?`
- 입력 중이면 아래에 **자동완성 드롭다운** (품목명 + aliases 부분 일치, 최대 8개)
- Enter 또는 버튼 클릭 → `/result?q=...`

### 4-3. 카테고리 그리드

- 검색바 아래, 데스크톱 6열 / 태블릿 4열 / 모바일 3열
- 각 셀 = 세로 스택: **정사각형에 가까운 흰 카드(radius 20px, soft shadow) 안에 아이콘** → 그 아래 라벨
  (라벨은 카드 바깥에 위치. 카드 안에 넣지 마라.)
- 라벨: 14px, `font-weight: 700`, `--text` 색, 가운데 정렬, 두세 줄까지 자연스럽게 줄바꿈
- hover: 카드가 `translateY(-4px)` + 그림자 강화, transition 150ms
- 클릭 → `/category/[id]`

**카테고리 17종 (id / 라벨 / lucide 아이콘 제안):**

| id | 라벨 | 아이콘 |
|---|---|---|
| paper | 종이 | `FileText` |
| paper-pack | 종이팩 | `Milk` |
| glass | 유리병 | `Wine` |
| metal | 금속류 | `CookingPot` |
| pet | 무색페트병 | `Bottle`(없으면 `Milk` 변형) |
| plastic-container | 합성수지 용기류 | `Package` |
| plastic-film | 합성수지 비닐류 | `ShoppingBag` |
| styrofoam | 발포합성수지 (스티로폼 등) | `Box` |
| textile | 의류 및 원단 | `Shirt` |
| battery | 전지류 | `BatteryCharging` |
| lighting | 조명제품 | `Lamp` |
| electronics | 전기전자제품 | `Monitor` |
| pesticide | 폐농약 | `SprayCan` |
| medicine | 폐의약품 | `BriefcaseMedical` |
| mercury | 수은함유 폐기물 | `Thermometer` |
| radioactive | 천연방사성제품 | `Radiation` |
| paint | 폐페인트, 광택제, 접착제 | `PaintBucket` |

아이콘 색은 카테고리 성격에 맞춰 **그린 계열(재활용 가능)** 과 **차콜/앰버 계열(전용 수거 필요)** 로 구분하면 좋다.

### 4-4. 결과 화면 (`/result`, `/item/[id]`)

한 화면에 위에서부터:
1. 품목명 + 최종 배출 분류 배지
2. **구성요소 분해** — 페트병이면 `본체 / 뚜껑 / 라벨`처럼 부위별 재질과 배출처를 카드로 나열
3. **행동 체크리스트** — 번호 + 동사 명령형 문장 + 체크박스. 진행률 바 상단 고정.
4. 지역 선택 드롭다운 + 해당 지역 예외 문구 (있을 때만)
5. **배출 완료 버튼** → 포인트 +10, 탄소 절감량 누적, 짧은 완료 애니메이션

모호한 품목은 2번 위치에 **되묻기 카드**를 띄우고, 사용자가 답을 고르기 전까지 체크리스트를 잠근다.

---

## 5. 데이터 모델

`data/items.json`:

```jsonc
{
  "categories": [
    { "id": "pet", "label": "무색페트병", "icon": "Milk", "tone": "green" }
  ],
  "items": [
    {
      "id": "pet-bottle-clear",
      "name": "투명 페트병",
      "categoryId": "pet",
      "aliases": ["생수병", "페트병", "물병", "음료수병"],
      "parts": [
        { "name": "본체", "material": "투명 PET", "disposal": "무색페트병 전용 수거함" },
        { "name": "뚜껑", "material": "PP (플라스틱류)", "disposal": "플라스틱류" },
        { "name": "라벨", "material": "비닐류", "disposal": "비닐류 (제거 대상)" }
      ],
      "steps": [
        { "action": "내용물을 완전히 비우세요", "detail": "음료가 남아 있으면 다른 자원까지 오염됩니다" },
        { "action": "물로 한 번 헹구세요", "detail": "" },
        { "action": "라벨을 떼어 비닐류에 버리세요", "detail": "절취선을 잡아당기면 쉽게 분리됩니다" },
        { "action": "발로 밟아 압착하고 뚜껑을 닫으세요", "detail": "부피를 줄여 수거 효율을 높입니다" },
        { "action": "무색페트병 전용함에 넣으세요", "detail": "" }
      ],
      "ambiguity": {
        "question": "병 색깔이 투명한가요?",
        "options": [
          { "label": "투명해요", "resultItemId": "pet-bottle-clear" },
          { "label": "색이 들어있어요", "resultItemId": "pet-bottle-colored" }
        ]
      },
      "regionNotes": { "default": "", "서울특별시": "일부 자치구는 요일제로 배출일이 지정됩니다" },
      "carbonSavingG": 45,
      "point": 10
    }
  ]
}
```

**초기 데이터는 최소 30개 품목**을 채워라. 반드시 포함할 어려운 케이스:
투명 페트병 / 유색 페트병 / 치약 튜브 / 기름 묻은 피자박스 / 배달 플라스틱 용기 / 테이프 붙은 택배박스 / 스티로폼 아이스박스 / 우유팩 / 종이컵 / 즉석밥 용기 / 칫솔 / 볼펜 / 아이스팩 / 깨진 유리 / 화장품 공병 / 알약 / 건전지.

---

## 6. 작업 순서 (Phase 별로 끊어서 진행)

**Phase 1 — 골격과 UI**
1. Next.js + TypeScript + Tailwind 프로젝트 세팅, 디자인 토큰 정의
2. 메인 페이지: 검색바 + 카테고리 그리드 (데이터는 하드코딩 mock 가능)
3. 반응형 확인 (360px / 768px / 1280px)
→ 여기까지 하고 결과 보여준 뒤 멈춰라.

**Phase 2 — 검색과 가이드**
4. `data/items.json` 작성 (30개 이상)
5. 자동완성 + 검색 결과 페이지 + 카테고리 상세 페이지
6. 구성요소 카드, 행동 체크리스트, 되묻기 분기, 지역 드롭다운
7. 검색어가 데이터에 없을 때의 빈 상태 화면

**Phase 3 — 기록과 AI**
8. localStorage 기반 포인트/탄소 절감 누적 + 마이페이지(간단한 숫자 요약 정도)
9. `/api/ask` route handler: JSON에 없는 자유 질문을 Claude에게 넘겨 위 데이터 모델과 **동일한 스키마의 JSON**으로 응답받아 같은 UI로 렌더
   - 시스템 프롬프트에 원칙 3가지를 명시하고, 확신이 없으면 `ambiguity` 필드를 채워 되묻도록 지시
   - API 키가 없으면 Phase 2의 로컬 검색으로 조용히 폴백

---

## 7. 완료 기준

- [ ] 메인 화면에 검색바와 카테고리 그리드 외의 요소가 없다
- [ ] "페트병" 검색 → 3초 안에 체크리스트까지 도달한다
- [ ] 모든 안내 문구가 명사형이 아닌 **동사 명령형**이다
- [ ] 모호한 품목에서 되묻기가 실제로 작동한다
- [ ] 체크리스트 완료 → 포인트·탄소 수치가 실제로 증가하고 새로고침 후에도 유지된다
- [ ] 모바일 360px에서 가로 스크롤이 생기지 않는다
- [ ] `npm run build` 가 경고 없이 통과한다

## 8. 코드 규칙

- 컴포넌트는 `components/` 아래, 한 파일 200줄 이하
- 서버 컴포넌트 기본, 인터랙션 필요한 곳만 `"use client"`
- 하드코딩된 색상값 금지 — 전부 CSS 변수 참조
- 주석과 커밋 메시지는 한국어
